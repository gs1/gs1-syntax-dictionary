---
name: Super Linter

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  statuses: write

jobs:

  superlinter:

    runs-on: ubuntu-latest

    permissions:
      contents: read
      statuses: write

    steps:

      - uses: actions/checkout@v6
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: Load disabled linters
        run: >
          grep -v '^#' .github/linters/superlinter-disabled.env
          | grep '=' >> "$GITHUB_ENV"

      - name: Super Linter
        uses: super-linter/super-linter@v8
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
          VALIDATE_ALL_CODEBASE: true
          # IGNORE_GITIGNORED_FILES is omitted: it makes Super Linter pass
          # the --gitignore option, which jscpd 5 does not support:
          # https://github.com/super-linter/super-linter/issues/7949
          SAVE_SUPER_LINTER_SUMMARY: true
          FILTER_REGEX_EXCLUDE: "\
            (docs/\
            |src/build\
            |src/acutest\\.h\
            |corpus-\
            |src/slow-unit-)"
